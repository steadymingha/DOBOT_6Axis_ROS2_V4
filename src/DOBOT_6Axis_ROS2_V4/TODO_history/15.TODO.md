# TODO

## Goal
기존 cr.world(shelf/wirebonder/post_wb) Gazebo 환경에서 CR10을 MPO-700 위에 세워
jog로 리치·간섭을 눈으로 확인할 수 있는 `run_mpo700_cr10.sh`를 만든다. MoveIt은
포함하지 않는다. `arm_type` 하나만 바꾸면 cr12/16/20으로도 갈아끼울 수 있게 한다.
기존 CR7 실행 경로(run_mpo700_cr7.sh 및 그 자산)는 건드리지 않는다.

## Tasks

### 1. 정제된 팔 단독 xacro 생성 (arm-only, 결합용 + jog IK용) — cr10만
- [x] `cra_description/urdf/`에 `cr10_arm.xacro` 생성: base_link + Link1~6 + joint1~6만
      포함(dummy_link/world_joint/dummy_joint/gazebo/ros2_control 제거 → 없는
      cr10_moveit 참조 제거, base_link 부모 중복 방지).
- [x] `xacro` 처리로 에러 없이 파싱되는지 확인.
- [ ] cr12/16/20은 지금 만들지 않음. 나중에 같은 방식으로 `{arm}_arm.xacro`만
      추가하면 스왑 메커니즘(2~4단계)이 그대로 받아줌 — 그 절차를 참고사항에 기록.

### 2. 스왑형 결합 xacro
- [x] `cra_description/urdf/arm_on_mpo700.urdf.xacro` 생성: 기존 cr7_on_mpo700의
      MPO-700 body/cube/arm_mount_joint/gazebo ros2_control/gripper/d405 카메라를
      그대로 재사용하고, 인라인 CR7 팔 블록만 `<xacro:include>`로 교체.
- [x] `<xacro:arg name="arm_type" default="cr10"/>` 추가 →
      `${arm_type}_arm.xacro` include.
- [x] 컨트롤러 설정은 기존 `$(find cr7_moveit)/config/ros2_controllers.yaml`
      재사용(관절명 joint1~6 동일). 컨트롤러/그룹 이름 cr7_group_controller 유지.
- [x] `xacro arm_on_mpo700.urdf.xacro arm_type:=cr10`이 유효 URDF를 뱉는지 확인
      (base_link 부모 1개, 링크 트리 정상 — check_urdf 통과).

### 3. MoveIt 없는 Gazebo 런치
- [x] `dobot_gazebo/launch/gazebo_mpo700_arm.launch.py` 생성:
      gazebo_mpo700_cr7.launch.py를 기반으로 urdf를 arm_on_mpo700(arm_type 매핑)로
      바꾸고, cr.world 재사용, spawn_entity + 컨트롤러 로드 체인
      (joint_state_broadcaster → cr7_group_controller → gripper_controller) 유지.
      (box_l2c 스폰은 pick-place용이라 제외.)
- [x] `arm_type`을 환경변수 `ARM_TYPE`(기본 cr10)에서 읽어 xacro 매핑에 전달.
- [x] 기존 gazebo_mpo700_cr7.launch.py는 수정하지 않는다.

### 4. 실행 스크립트
- [x] `run_mpo700_cr10.sh` 생성: run_mpo700_cr7.sh를 기반으로 하되
      `export ARM_TYPE=cr10`, MoveIt/카메라뷰 단계 제외, gazebo_mpo700_arm.launch.py
      실행. conda-strip/GAZEBO_MODEL_PATH 등 환경 설정은 동일하게.
- [x] cr12/16/20으로 바꾸려면 스크립트 상단 `ARM_TYPE`만 바꾸면 되도록 주석 명시.

### 5. jog IK를 arm-aware로 (CR10 IK 정확도) — geometry.py 한 파일만
- [x] `cr7_pnp/geometry.py`의 `XACRO_PATH`/`COMBINED_XACRO`를 `os.getenv("ARM_TYPE","cr7")`
      기반 경로로 변경. 기본값 cr7 → 기존 CR7 동작 100% 불변.
- [x] XACRO_PATH는 arm-only가 필요하므로 `${ARM_TYPE}_arm.xacro`(1단계 산출물)를 가리키게.
      (단, 기본 cr7은 기존 `cr7_robot.xacro`를 계속 쓰도록 분기 — cr7 무변경 보장.)
- [x] `jog_tcp.py`는 수정하지 않음: 팔 경로를 하드코딩하지 않고 cr7_pnp에서 받으므로
      `ARM_TYPE=cr10 python3 jog_tcp.py`만으로 CR10 jog 동작.

### 6. 빌드 및 스모크 테스트
- [x] `colcon build --packages-select cra_description dobot_gazebo` (새 xacro/launch를 install/share로).
- [x] URDF 유효성: `check_urdf` 트리 정상(mpo_base_link→cube_link→base_link→Link1..6→gripper),
      base_link 부모 1개(arm_mount_joint), cr10 메시 14/cr7 메시 0.
- [x] 런타임 로드: robot_state_publisher가 arm_type=cr10 결합 URDF 전체 세그먼트
      파싱 성공(런치의 xacro 처리 end-to-end 확인).
- [x] jog 모델: pinocchio가 cr10_arm(IK 6DOF, Link6 프레임) + arm_on_mpo700(충돌 190쌍)
      빌드 성공 → jog의 IK/충돌 모델 로드 확인.
- [x] 라이브 Gazebo: `./run_mpo700_cr10.sh` → cr10_on_mpo700 스폰 성공,
      컨트롤러 3개(joint_state_broadcaster/cr7_group_controller/gripper_controller) active,
      `/joint_states`에 joint1~6 게시 확인. (첫 실패는 잔여 gazebo 인스턴스/포트 11345
      충돌이었고 환경 한계 아님 — 깨끗한 상태에서 정상 동작.)
- [ ] jog 이동은 대화형(키보드)이라 사용자가 직접:
      `ARM_TYPE=cr10 ~/dobot_ws/.venv/bin/python3 tools/jog_tcp.py`.

## 참고사항
- 마운트 높이(base_link 바닥 1.0 m)는 CR7 기준으로 튜닝됨. CR10/12/16/20은 베이스가
  더 커서 큐브에 살짝 겹쳐 보일 수 있음(리치·jog 검증엔 무해). 필요 시 arm_mount_joint
  z만 조정 — 이번 범위에는 넣지 않음.
- 컨트롤러 yaml·관절명·컨트롤러 이름을 CR7과 동일하게 유지 → jog_tcp/시퀀스 코드
  변경 최소화(5단계 geometry.py 외 없음).
- CR7 자산(run_mpo700_cr7.sh, cr7_on_mpo700.urdf.xacro, gazebo_mpo700_cr7.launch.py)은
  일절 수정하지 않음. geometry.py만 기본값 cr7로 하위호환 변경.
- 링크명이 arm마다 동일(base_link, Link1~6)해 cr7_robot.srdf 계열 가정도 유지됨.
- cr12/16/20 추가 절차(나중에): (1) `{arm}_arm.xacro` 생성(1단계와 동일),
  (2) `run_mpo700_cr10.sh` 복사해 `ARM_TYPE`만 바꿈(또는 상단 변수 수정),
  (3) `colcon build --packages-select cra_description`. 코드 변경 불필요.
