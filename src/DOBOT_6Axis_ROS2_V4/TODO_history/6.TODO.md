# TODO

## Goal
cr.world의 ARIAC 선반을 제거하고 src/blender/ 의 새 선반·박스·ArUco 모델로 교체한다.

## Tasks

### 1. GAZEBO_MODEL_PATH 설정
- [x] `run_mpo700_cr7.sh`에 `export GAZEBO_MODEL_PATH=$GAZEBO_MODEL_PATH:/home/user/dobot_ws/src/blender` 추가

### 2. cr.world 기존 모델 제거
- [x] ARIAC 선반 `<model name="shelf">` 블록 제거
- [x] `<model name="pick_box">` 블록 제거
- [x] `<model name="place_marker">` 블록 제거
- [x] `<model name="workspace_limit">` 블록 제거

### 3. cr.world 새 모델 추가
- [x] 선반 `<include>` 추가 — `model://shelf`, pose `(0.8, 0, 0)`
- [x] 박스 1층 좌 추가 — `model://box`, entity `box_l1a`, pose `(0.7095, 0, 0.37)`, yaw 1.5708
- [x] 박스 1층 우 추가 — `model://box`, entity `box_l1b`, pose `(0.8905, 0, 0.37)`, yaw 1.5708
- [x] 박스 2층 좌 추가 — `model://box`, entity `box_l2a`, pose `(0.7095, 0, 0.67)`, yaw 1.5708
- [x] 박스 2층 우 추가 — `model://box`, entity `box_l2b`, pose `(0.8905, 0, 0.67)`, yaw 1.5708
- [x] ArUco 추가 — `model://aruco_box`, entity `aruco`, pose `(1.72, 0, 0.625)`, yaw 0

## 참고사항
- 선반 원점 (0.8, 0, 0) 기준: 보드 윗면 z = 0.30 / 0.60 / 0.90
- 박스 spawn z = 보드 윗면 + 0.07 (박스 원점이 기하 중심, 반높이 0.07)
- ArUco spawn z = 보드 윗면(0.60) + 0.025 = 0.625 (aruco_box 반높이 0.025)
- 박스는 동적(non-static) — world 로드 시 선반도 함께 존재하므로 world 파일 내 배치 가능
- model:// URI 해석에 GAZEBO_MODEL_PATH = /home/user/dobot_ws/src/blender 필요
